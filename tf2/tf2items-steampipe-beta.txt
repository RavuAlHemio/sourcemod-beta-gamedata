/*
 * Save this file as:
 * tf/addons/sourcemod/gamedata/tf2.items.txt
 * and make it read-only.
 * (Windows: right-click -> Properties -> General -> tick Read only)
 * (Linux/OS X: chmod a-w sm-tf2.games.txt)
 *
 * ONCE THE TF2 STEAMPIPE UPDATE LEAVES BETA, MAKE THE FILE WRITABLE AGAIN!
 * (Windows: uncheck Read only)
 * (Linux/OS X: chmod u+w tf2.items.txt)
 * It (deliberately) overrides SourceMod's gamedata auto-update feature.
 *
 * This file has been modified by Ravu al Hemio.
 * Credit goes to Fyren and psychonic for the gdc tool.
 */
"Games"
{
	"tf"
	{
		"Offsets"
		{
			"GiveNamedItem"
			{
				"windows"	"399"
				"linux"	"400"
				"mac"	"400"
			}
		}
	}
}
